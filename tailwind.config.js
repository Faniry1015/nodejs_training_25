export const content = [
    './templates/**/*.ejs', // Inclut tous les fichiers EJS dans le dossier templates
    './public/shared/**/*.css', // Inclut tous les fichiers CSS dans le dossier public
];
export const theme = {
    extend: {
        // Ajoutez ici vos personnalisations si nécessaire
    },
};
export const plugins = [];
