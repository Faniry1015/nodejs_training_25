<% include('./shared/header.ejs', { title: 'Apprendre Fastify' }) %>

<h1><%= pageTitle %></h1>
<% if (locals.posts) { %>
  <% for (post of posts) { %>
  <h2><%= post.title %></h2>
  <p><%= post.content %></p>
  <% } %> <% } %>

<% include('./shared/footer.ejs') %>
 